import 'package:flutter/material.dart';

class AddNewsScreen extends StatelessWidget {
  static const routeName = 'AddNewsScreen';
  
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(title: Text('Update news')), body: Container());
  }
}
